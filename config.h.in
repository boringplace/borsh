/**
 * \file config.h
 *
 * \brief Boring shell configuration header
 */

#if !defined(BORSH_CONFIG_H)
#define BORSH_CONFIG_H

#define VERSION "${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}.${PROJECT_VERSION_PATCH}"
#define PACKAGE "${BORSH_PACKAGE}"
#define PACKAGE_NAME "${BORSH_PACKAGE_NAME}"
#define PACKAGE_BUGREPORT "${BORSH_PACKAGE_BUGREPORT}"
#define USER_PREFIX "${USER_PREFIX}"
#define REGISTER_USERNAME "${REGISTER_USERNAME}"
#define PEOPLE_DIR "${PEOPLE_DIR}"
#define CMD_DIR "${CMD_DIR}"

/**
 * \brief Path to pending registration users' database.
 */
#define BORSH_DBPATH "${BORSH_DBPATH}"

#endif /* BORSH_CONFIG_H */

